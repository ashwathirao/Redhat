image:
  file: .gitpod.Dockerfile

tasks:
  - before: clear && echo 'Wel...'
    init: echo 'hello'
    command: echo 'welcome'

vscode:
  extensions:
    - k--kato.intellij-idea-keybindings
    - https://github.com/redhat-developer/vscode-java/releases/download/v1.7.0/java-1.7.0.vsix
    
